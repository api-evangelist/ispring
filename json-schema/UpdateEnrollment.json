{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "UpdateEnrollment",
  "required": [
    "accessDate",
    "dueDateType"
  ],
  "properties": {
    "accessDate": {
      "type": "string",
      "format": "datetime"
    },
    "dueDateType": {
      "$ref": "#/components/schemas/DueDateType"
    },
    "dueDate": {
      "type": "string",
      "format": "date"
    },
    "duePeriod": {
      "type": "integer"
    },
    "lockAfterDueDate": {
      "type": "boolean"
    }
  },
  "type": "object",
  "xml": {
    "name": "request"
  }
}